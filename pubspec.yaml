name: count_up_app_with_state_notifier_and_gold_test
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.0-262.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^1.0.1
  json_serializable: ^6.0.1
  freezed_annotation: ^1.0.0
  audioplayers: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  freezed: ^1.0.0
  build_runner: ^2.1.5
  golden_toolkit: ^0.12.0
  mocktail: ^0.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/up.mp3
    - assets/sounds/down.mp3
    - assets/sounds/reset.mp3
    - assets/FontManifest.json
